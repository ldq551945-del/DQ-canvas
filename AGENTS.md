# AGENTS.md

本文档约束 DQ-绘图仓库中的 AI 与自动化开发行为。目标是让改动可运行、可验证、可交付，同时保护现有业务契约、用户数据和未提交工作。

适用范围为本文件所在目录及其子目录；更深层的 `AGENTS.md` 可补充局部规则。平台安全规则与用户当前明确要求优先于本文件；发生冲突时说明冲突并采用风险更低、证据更充分的路径。

## 1. 工作方式

- 先读后改。先检查相关代码、配置、文档、测试和调用方，再形成判断；结论应能追溯到文件或命令输出。
- 默认推进到可交付状态。实现类任务完成代码、必要文档和与风险匹配的验证；分析、评审和排查任务给出证据、影响范围与可执行结论。
- 保持改动聚焦。修改前执行 `git status --short`，识别用户已有改动；不得回滚、覆盖或顺手格式化无关文件。
- 优先最小充分实现。修复根因，不为假想兼容场景增加分支，不引入没有当前收益的抽象。
- 不确定但可低风险验证时直接调查；会改变业务语义、数据格式、权限边界、部署拓扑或外部状态时，先说明影响与选择，不擅自假设。
- 不伪造成功，不静默吞错，不用假数据或无依据默认值掩盖失败。无法完成某项验证时，交付中写明缺口和原因。
- 反复出现且长期有效的约束才补充到本文件；阶段性需求、具体功能状态和一次性验收项写入对应文档或测试，不让根规则无限膨胀。

## 2. 项目事实与入口

版本号和依赖以锁文件、清单与 CI 为准，不以本文中的文字描述代替实际配置。

- 主应用位于 `web/`：Next.js App Router、React、TypeScript strict、Ant Design、Tailwind CSS、Zustand、Vitest，使用 pnpm。
- 页面、布局和 Route Handler 位于 `web/src/app/`；登录后工作区位于 `web/src/app/(user)/`，管理后台位于 `web/src/app/admin/`。
- 服务端业务、任务编排、Provider、计费、媒体和安全逻辑位于 `web/src/lib/server/`；数据库 Schema 与 Repository 位于 `web/src/lib/server/database/`。
- 浏览器访问本站 API 的客户端位于 `web/src/services/api/`；跨页面共享的客户端瞬时状态位于 `web/src/stores/`。
- Canvas 页面、组件、页面私有 Hook、状态和工具位于 `web/src/app/(user)/canvas/`；跨页面领域契约才上移到 `web/src/lib/`。
- 文档站位于 `docs/`，详细文档位于 `docs/content/docs/`；主应用默认端口为 `3000`，文档站默认端口为 `3001`。
- 可选抠图 Sidecar 位于 `services/rembg/`，使用 Python、FastAPI 与 rembg；它是内部服务，不得直接暴露到公网。
- PostgreSQL 是默认主库；JSON Provider 是项目已有的单机文件模式。用户业务数据保存在服务端，不依赖浏览器业务缓存。
- 媒体写入 `DQ_DATA_DIR` 或可选 S3 兼容对象存储。环境变量、运行方式和部署差异以 `.env.example` 与 `docs/content/docs/overview/configuration.mdx` 为准。

开始陌生模块前，优先读取：

- `docs/index.md`：文档短索引。
- `docs/content/docs/overview/project-structure.mdx`：目录职责与核心请求链路。
- `docs/content/docs/backend/api-response.mdx`：现有接口响应与敏感配置契约。
- `docs/content/docs/backend/backend-database.mdx`：数据库结构与 Repository 边界。
- `docs/content/docs/progress/todo.mdx` 与 `docs/content/docs/progress/pending-test.mdx`：当前待办和待验收状态。

## 3. 代码与架构边界

### 3.1 通用质量

- 页面、Route Handler、Service、Repository 和 Provider 只承担本层职责。纯算法、协议转换和可独立测试的业务规则放入同域 `lib`、service 或 util。
- Route Handler 只做请求解析、身份与权限校验、调用服务、HTTP 状态和响应映射；不得堆积 SQL、计费、任务状态机或上游协议细节。
- Repository 只负责参数化查询、事务和持久化映射；业务不变量在 Service/领域层表达，唯一性、并发和原子性由数据库约束与事务兜底。
- 不新增只改名或透传 props 的组件，不用零散 helper 掩盖超长主流程。先识别职责，再拆分入口、编排、纯逻辑和副作用。
- 页面私有组件与 Hook 放在页面目录内；只有两个以上页面真实复用的能力才上移到 `web/src/components/` 或 `web/src/hooks/`。
- 已有全局 Store、Hook、主题或配置入口时直接复用，不通过多层 props 透传。Store 只承载需要共享或订阅的状态，复制、下载、确认等 UI 副作用优先使用 Hook。
- 新文件宜控制在约 500 行内；文件已超过约 800 行时，新增复杂功能前优先抽出明确职责。历史超长文件按任务影响面渐进治理，不做无关的大爆炸重构。
- 核心业务入口、非直观算法、安全边界、并发理由和降级策略使用简短中文注释说明“为什么”；显而易见的赋值与流程不写注释。
- 标准格式、协议、解析、压缩、加密、日期和媒体处理优先使用项目已有或成熟依赖，不重复手写底层实现。
- 错误语义分层：只读展示路径可在有日志或用户提示时降级；保存、生成、计费、权限、删除、密钥和配置写入必须强校验并明确失败。

### 3.2 API 与 Provider

- 现有 API 响应并未全局统一：部分接口使用 `{ code, data, msg }`，部分使用 `{ error }` 或媒体流/重定向。修改时保持调用方和相邻路由的既有契约，并以 `api-response.mdx` 为准；不得顺手全局改形状。
- 新接口优先沿用所属业务域的响应 helper、鉴权、分页、错误映射和测试写法。输入在边界校验长度、类型、枚举、归属、限流和请求体大小。
- 支持 PostgreSQL 与 JSON Provider 的业务必须保持两者语义一致；只支持 PostgreSQL 的能力应显式拒绝或隐藏，不能假装成功或写入不完整文件状态。
- 调整 Schema、表、索引、查询或事务时同步修改 `web/src/lib/server/database/schema.ts`、相关 Repository、同域测试和 `backend-database.mdx`。是否需要迁移或重建数据库必须依据当前项目发布策略并明确说明，不能自行删除数据或卷。
- 页面不得直接访问数据库或外部模型；浏览器 API 客户端不得拼接真实上游地址、渠道鉴权或数据库细节。

## 4. 核心业务不变量

以下规则属于高风险契约。涉及它们时至少按跨文件任务处理，并检查入口、调用方、持久化、恢复路径和相邻测试。

### 4.1 身份、权限与敏感信息

- 私有资源的读取、修改和删除必须校验当前用户及资源归属；管理员能力显式校验角色并记录项目既有审计信息。无权访问时沿用所属接口的 403/404 隐藏策略。
- 系统渠道 API Key、数据库凭据、支付密钥、维护令牌和 Webhook 密钥只在服务端环境变量或加密存储中读取。浏览器只接收“是否已配置”等脱敏状态，不回显真实值。
- 响应、日志、异常和测试快照不得包含完整密钥、Authorization、Cookie、签名 URL、对象 Key、数据库连接串或上游可能回显的鉴权字段。
- 外部 URL、媒体代理、Webhook、重定向和上传入口必须保留现有 SSRF、签名、来源、大小、MIME、Range、超时、并发和限流保护，不为调试临时绕过。

### 4.2 生成任务、Agent 与上游调用

- 服务端持久任务是状态事实源；前端、Canvas 节点 metadata 和本地状态只负责展示与关联，不得覆盖服务端终态。
- 用户每次主动提交使用新的稳定请求身份；同一请求的重复送达按幂等键返回原记录。只有用户对明确失败结果发起显式重试时才创建新的 attempt。
- 一旦向上游发起过创建请求，自动恢复、页面唤醒、Worker 接管、查询超时或进程重启只能继续查询已保存的上游任务 ID，不得再次创建同类上游任务。
- `pending` / `running` 不得仅因 `updatedAt` 过旧或执行器心跳中断被判失败；`result_ready` 只重试媒体持久化。只有上游明确终态失败或用户授权的人工处理才能进入可重试路径。
- 逻辑模型 ID 是前端、定价和任务记录的稳定身份；渠道、上游模型、地址和密钥由服务端路由。候选切换、协议适配和失败回退不能对同一候选造成重复请求。
- Agent 规划提示、改写提示、模型选择理由、创作简报和复盘细节只用于内部执行，不进入用户可见的生成型对话。用户原文与上游执行提示必须分别持久化和展示，禁止互相回退冒充。
- 图片、视频、Canvas 与短剧尺寸沿用统一优先级：用户本轮明确尺寸 > 当前精确自定义宽高 > 本轮参考图比例 > Agent 规划尺寸 > 后台默认。读取 Canvas 时使用真实紧凑快照；不要用根级默认字段覆盖节点配置中的精确尺寸。
- 自定义媒体尺寸只校验为可解析的正整数，不额外伪造平台统一模型上限；解码、内存与重采样可保留独立资源保护，上游能力拒绝应返回脱敏后的真实错误。
- 视频时长在请求上游前按最终逻辑模型能力归一化，并持久化实际发送值；上游成功后不得仅因轻微成片时长差异把任务改判失败。

### 4.3 计费、积分与支付

- 预估、扣费、退款和后台展示复用同一逻辑模型定价解析；不得分别按逻辑模型和上游别名计算。
- 扣费、退款、订单状态、优惠、邀请奖励和支付回调必须具备稳定幂等键，并在同一事务内更新余额/权益与流水。并发请求不能造成重复扣费、重复退款或部分成功。
- 订单保存的价格快照和 `amountCents` 是支付、退款与对账事实源，后续不得按当前活动或价格重新计算历史订单。
- 单价为 `0` 的调用仍记录幂等流水和套餐用量；上游失败时按原流水撤销相应用量，不能用 `pointsCost > 0` 判断是否需要退款。
- 不使用真实支付、短信、邮件或模型渠道做验证，除非用户明确授权且测试账号、额度和回滚路径已确认。

### 4.4 数据与媒体

- 创作会话、工作台记录、Canvas、素材库、短剧、生成任务、Agent 事件和媒体元数据保存在服务端并按用户恢复。浏览器只保存页面瞬时状态和主题等极小偏好；不得用 `localStorage`、IndexedDB 或 localforage 持久化业务列表、生成记录、图片、base64 或大 JSON。
- 创作会话与消息列表使用服务端分页；打开长会话时默认加载最新消息并支持向前加载历史，不能固定读取最早 N 条导致新消息在刷新后消失。
- 业务记录只保存稳定 `storageKey` 和站内媒体 API URL，不保存 base64、对象存储 Key 或临时签名 URL。已登记的永久媒体直接复用，不先下载再上传制造副本。
- 作品、素材、提示词封面、本地媒体和外部存储等内容图使用限宽 WebP 变体预览；只有明确下载操作返回原件。Logo、ICO 与 SVG 品牌资源不受此规则限制。
- 每个媒体写入必须登记用户归属、来源、原文件名、Provider，以及可用的会话、Run、任务或项目关联。用户读取与提供给外部上游前均校验归属；外部上游只获得短期签名地址。
- 外部存储开关只决定新媒体写入位置：开启后写 S3 兼容存储，关闭后写 `DQ_DATA_DIR`。历史媒体始终按自身 `storage_provider` 读取，不能随当前开关切换。
- 删除媒体前检查创作会话、素材库、Canvas、短剧、作品和生成记录引用；管理员批量删除、过期清理和迁移也不得用 `force` 绕过引用保护。对象上传和登记成功前不能删除本地源文件。
- `.env`、`.data/`、数据库、媒体原件、备份、日志、构建产物、模型权重、临时截图和个人工作目录不得提交 Git。

## 5. 前端与 Canvas 规范

### 5.1 通用 UI

- 页面文案默认中文；站点 Logo、favicon、manifest、登录页、用户端、后台和助手头像统一使用 DQ-绘图品牌入口，功能性图标按语义使用 `lucide-react` 或项目已有 Ant Design 图标。
- 主题优先复用 `web/src/lib/app-theme.ts`、`AppProviders`、`canvasThemes`、`useThemeStore` 和 Ant Design token。只引用仓库中真实存在的主题或 CSS token；不得把未落地的设计系统写进组件。
- 页面私有样式使用 Tailwind、CSS Module 或少量内联样式；`globals.css` 与 `web/src/app/styles/` 只承载变量、重置、跨页面布局和必要的第三方覆盖。
- 所有按钮、菜单项和自定义可点击元素提供清晰的浅色、深色、hover、focus、disabled 和危险状态。关键操作不能只靠隐藏图标、双击或右键发现。
- UI 改动同时检查桌面与窄屏、浅色与深色、键盘焦点、文本溢出和滚动边界。移动端重点检查 390px 与 430px；Drawer/Modal 不得超过视口或裁切底部操作。
- 全局 `html, body` 使用固定视口和内部滚动。独立页面沿用 `.app-scroll-page`，工作区长页面提供 `h-full min-h-0 overflow-y-auto` 或同等容器；隐藏滚动条时仍必须保留滚轮、触控与键盘滚动。
- 后台管理页沿用项目现有 SaaS 工作流：列表/卡片配合创建按钮和 Modal/Drawer；短字段使用紧凑响应式网格，密钥、证书和长文本独占通栏。用户身份展示使用公开账号 ID，不向界面泄露内部 UUID。
- 参考第三方 UI 只能吸收交互与视觉方向，必须改造成当前 Next.js/React/Tailwind/Ant Design 写法；不得直接复制许可证不明的源码、图片、字体、图标、素材或专有 token。

### 5.2 Canvas

- Canvas 组件沿用当前 `canvasThemes`、工具栏、节点、Modal 和浮层语言，避免硬编码造成明暗主题失配；高频操作保持紧凑，批量结果和助手面板不得长期遮挡主要创作区域。
- 图片节点默认保持原始比例。生成、导入和历史恢复缺少宽高时，图片加载后读取自然尺寸，保持节点中心不变地回写并持久化；明确自由变形的节点不得被自动改回比例。
- 节点预览面不得吞掉根节点拖动。只有按钮、输入框和媒体控件等明确交互区阻止冒泡；Modal、Popover 与查看器 Portal 必须隔离返回组件树的点击、指针、滚轮和右键事件。
- 画布交互修改按影响面覆盖节点拖动、节点内控件、连接与选择、弹层、缩放/平移或保存恢复中的相关路径。不能只用 class 字符串断言代替真实布局；尺寸问题读取浏览器 `getBoundingClientRect()`。
- 动效只表达状态和空间关系，尊重 `prefers-reduced-motion`；避免持续占用性能或干扰创作的装饰动画。

## 6. 文档与状态维护

- 根 `README.md` 保留项目定位、核心功能、快速开始、数据安全和文档入口；实现细节放入 `docs/content/docs/` 对应页面。
- `docs/index.md` 是短索引；目录职责维护在 `project-structure.mdx`，接口契约维护在 `api-response.mdx`，数据库维护在 `backend-database.mdx`。
- 新功能完成但尚未由用户验收时写入 `pending-test.mdx`；已确认的稳定能力再进入 `features.mdx`。完成 TODO 时先从 `todo.mdx` 移入 `pending-test.mdx`，不能直接宣称正式完成。
- `CHANGELOG.md` 的 `Unreleased` 只做版本级业务归纳，不逐条复制实现或待测试清单。
- 只有行为、接口、配置、部署或项目状态发生变化时才更新相应文档；纯内部重构或无状态变化不机械改文档。
- 涉及支付、合规、用户权利、运营、监控、备份、恢复或生产部署时，核对 `docs/content/docs/overview/production-readiness.mdx`，不能把“代码具备入口”写成“生产已经验证”。
- 文档默认中文，不写未经验证的承诺，不包含密钥、Token、内部地址、个人路径或机器敏感信息。

## 7. 环境与运行

- 先从 `README.md`、`web/package.json`、`docs/package.json`、锁文件和 `.github/workflows/quality.yml` 确认 Node.js、pnpm、PostgreSQL 与命令版本；不要凭记忆使用旧栈或旧命令。
- 本地开发从 `web/` 启动主应用，从 `docs/` 启动文档站。不得把文档站的 `3001` 当成主应用 `3000`。
- 本地数据目录通过 `DQ_DATA_DIR` 配置。启动前确认目标目录和数据库，避免无意切换账号数据；不得引入旧项目的 `CANVAS_BACKEND_DATA_DIR`、`backend/data` 等不存在约定。
- Docker、宝塔、外部数据库和低内存 Compose 是不同拓扑。部署改动检查公共默认值没有被某一拓扑的 host 网络、代理层数或数据库地址污染。
- `services/rembg/` 的资源、模型白名单、认证、像素/字节上限、并发与取消契约同时受 Sidecar 和 Next.js BFF 保护；修改一侧时检查另一侧和 Compose 配置。
- 删除数据库、卷、媒体目录、对象存储资源，执行真实迁移、发布、支付回调或外部模型调用属于高风险操作；先解析精确目标、说明影响与恢复方式，并取得用户明确授权。

## 8. 验证纪律

验证从最相关、成本最低的检查开始，再按影响面扩大；不能用一次全量命令代替对关键业务路径的理解。

### 8.1 最小验证矩阵

- 纯 Markdown/规则文档：检查路径、命令和事实与仓库一致，运行 `git diff --check`；MDX、文档导航或文档应用代码变化再运行 Docs 类型检查与构建。
- TypeScript 纯逻辑：运行同域 Vitest 测试和 `pnpm run typecheck`；没有相邻测试但已有测试体系时补充最小回归测试。
- Route Handler / Service：运行路由与服务测试、类型检查，并验证鉴权、输入错误、成功响应、持久化和错误映射；外部依赖用项目既有 mock/fixture。
- UI：运行相关组件/工具测试、类型检查和 `pnpm run build`，并在真实浏览器验证受影响页面。Canvas 和响应式改动检查桌面/移动端、浅色/深色与关键交互。
- 数据库、权限、计费、媒体删除和任务恢复：除相关测试与类型检查外，检查事务、幂等、并发、Provider 差异、失败回滚和调用链；说明 Schema/部署/数据风险。
- Docker、构建或部署配置：运行相邻 contract 测试或发布检查，核对所有受支持 Compose 拓扑和 standalone 产物。
- rembg Sidecar：在依赖可用时从 `services/rembg/` 运行 `python -m unittest test_app.py`，并运行受影响的 Next.js BFF/任务测试。

### 8.2 全量质量门禁

跨模块重构、发布准备、依赖或构建配置变更，以及用户明确要求全量验证时，按 CI 执行：

```bash
cd web
pnpm run typecheck
pnpm test
pnpm run format:check
pnpm run build

cd ../docs
pnpm run types:check
pnpm run build
```

- `web` 的 `pnpm run build` 已包含 TypeScript 检查；CI 可通过环境变量跳过重复检查，但本地不得据此省略未执行过的类型检查。
- 发布前从 `web/` 运行 `pnpm run check:release`，它会检查补丁、Compose/Render 契约、敏感运行目录、依赖审计、格式、类型和 standalone 构建；仍需人工验证它提示的关键页面。
- 真实模型、支付、短信、邮件、对象存储和生产数据库测试可能消耗额度或改变外部状态，默认不执行。使用 fixture/mock 验证本地契约，并在交付中列出未覆盖的真实上游矩阵。
- 同类失败连续三次时停止盲试，重新检查假设、环境、日志和最小复现，换路径后再继续。

## 9. Git、提交与发布

- 未经用户明确要求，不创建分支、不提交、不打 tag、不推送、不发布。
- 提交前只暂存本任务文件，检查 `git diff --check`、相关测试结果与敏感文件；不得把工作区其他人的改动混入提交。
- 新提交说明使用：

```text
<type>(<scope>): <业务模块> - <变更摘要>
```

- `type` 使用 `feat`、`fix`、`refactor`、`perf`、`docs`、`test`、`build`、`ci`、`chore` 或 `revert`；`scope` 使用 `canvas`、`platform`、`auth`、`api`、`database`、`deploy`、`release`、`dependencies`、`security`、`media`、`admin` 等技术域。
- 业务模块与摘要使用可检索的中文业务结果，不堆文件名，不加句末句号。例如：`fix(canvas): 模型选择 - 清空选项后不再保留失效模型`。
- 只有用户明确要求发布时，才整理 `CHANGELOG.md` 的 `Unreleased`、更新 `VERSION`、运行发布检查、创建发布提交与对应 `vX.Y.Z` tag。发布前先确认工作树中哪些改动属于本次版本，不能默认提交全部未提交文件。

## 10. 交付格式

任务结束时用简短中文说明：

- 完成了什么，以及解决的是结构性根因还是单点问题。
- 修改了哪些文件和外部契约是否变化。
- 实际运行了哪些测试、检查或浏览器路径，结果如何。
- 哪些验证未执行、原因与剩余风险。

只有在实现、验证和风险说明都与事实一致时，才称任务已完成。
