diff --git a/dist-cjs/lib/editor/managers/FontManager/FontManager.js b/dist-cjs/lib/editor/managers/FontManager/FontManager.js
index ab010c4..c7760ac 100644
--- a/dist-cjs/lib/editor/managers/FontManager/FontManager.js
+++ b/dist-cjs/lib/editor/managers/FontManager/FontManager.js
@@ -94,18 +94,27 @@
-  ensureFontIsLoaded(font) {
-    const existingState = this.getFontState(font);
-    if (existingState) return existingState.loadingPromise;
-    const instance = this.findOrCreateFontFace(font);
-    const state = {
-      state: "loading",
-      instance,
-      loadingPromise: instance.load().then(() => {
-        this.editor.getContainerDocument().fonts.add(instance);
-        this.fontStates.update(font, (s) => ({ ...s, state: "ready" }));
-      }).catch((err) => {
-        console.error(err);
-        this.fontStates.update(font, (s) => ({ ...s, state: "error" }));
-      })
-    };
-    this.fontStates.set(font, state);
-    return state.loadingPromise;
-  }
+  ensureFontIsLoaded(font) {
+    const existingState = this.getFontState(font);
+    if (existingState) return existingState.loadingPromise;
+    const instance = this.findOrCreateFontFace(font);
+    const isStale = () => this.fontStates.__unsafe__getWithoutCapture(font) !== state;
+    const state = {
+      state: "loading",
+      instance,
+      loadingPromise: instance.load().then(() => {
+        if (isStale()) {
+          console.debug(`Font "${font.family}" load interrupted by editor dispose`);
+          return;
+        }
+        this.editor.getContainerDocument().fonts.add(instance);
+        this.fontStates.update(font, (s) => ({ ...s, state: "ready" }));
+      }).catch((err) => {
+        if (isStale()) {
+          console.debug(`Font "${font.family}" load interrupted by editor dispose`, err);
+          return;
+        }
+        console.error(err);
+        this.fontStates.update(font, (s) => ({ ...s, state: "error" }));
+      })
+    };
+    this.fontStates.set(font, state);
+    return state.loadingPromise;
+  }
diff --git a/dist-esm/lib/editor/managers/FontManager/FontManager.mjs b/dist-esm/lib/editor/managers/FontManager/FontManager.mjs
index 493b80b..dea7b7c 100644
--- a/dist-esm/lib/editor/managers/FontManager/FontManager.mjs
+++ b/dist-esm/lib/editor/managers/FontManager/FontManager.mjs
@@ -76,18 +76,27 @@
-  ensureFontIsLoaded(font) {
-    const existingState = this.getFontState(font);
-    if (existingState) return existingState.loadingPromise;
-    const instance = this.findOrCreateFontFace(font);
-    const state = {
-      state: "loading",
-      instance,
-      loadingPromise: instance.load().then(() => {
-        this.editor.getContainerDocument().fonts.add(instance);
-        this.fontStates.update(font, (s) => ({ ...s, state: "ready" }));
-      }).catch((err) => {
-        console.error(err);
-        this.fontStates.update(font, (s) => ({ ...s, state: "error" }));
-      })
-    };
-    this.fontStates.set(font, state);
-    return state.loadingPromise;
-  }
+  ensureFontIsLoaded(font) {
+    const existingState = this.getFontState(font);
+    if (existingState) return existingState.loadingPromise;
+    const instance = this.findOrCreateFontFace(font);
+    const isStale = () => this.fontStates.__unsafe__getWithoutCapture(font) !== state;
+    const state = {
+      state: "loading",
+      instance,
+      loadingPromise: instance.load().then(() => {
+        if (isStale()) {
+          console.debug(`Font "${font.family}" load interrupted by editor dispose`);
+          return;
+        }
+        this.editor.getContainerDocument().fonts.add(instance);
+        this.fontStates.update(font, (s) => ({ ...s, state: "ready" }));
+      }).catch((err) => {
+        if (isStale()) {
+          console.debug(`Font "${font.family}" load interrupted by editor dispose`, err);
+          return;
+        }
+        console.error(err);
+        this.fontStates.update(font, (s) => ({ ...s, state: "error" }));
+      })
+    };
+    this.fontStates.set(font, state);
+    return state.loadingPromise;
+  }
diff --git a/src/lib/editor/managers/FontManager/FontManager.ts b/src/lib/editor/managers/FontManager/FontManager.ts
index 37c7adb..f86ed64 100644
--- a/src/lib/editor/managers/FontManager/FontManager.ts
+++ b/src/lib/editor/managers/FontManager/FontManager.ts
@@ -104,23 +104,37 @@
-	ensureFontIsLoaded(font: TLFontFace): Promise<void> {
-		const existingState = this.getFontState(font)
-		if (existingState) return existingState.loadingPromise
-
-		const instance = this.findOrCreateFontFace(font)
-		const state: FontState = {
-			state: 'loading',
-			instance,
-			loadingPromise: instance
-				.load()
-				.then(() => {
-					this.editor.getContainerDocument().fonts.add(instance)
-					this.fontStates.update(font, (s) => ({ ...s, state: 'ready' }))
-				})
-				.catch((err) => {
-					console.error(err)
-					this.fontStates.update(font, (s) => ({ ...s, state: 'error' }))
-				}),
-		}
-
-		this.fontStates.set(font, state)
-		return state.loadingPromise
-	}
+	ensureFontIsLoaded(font: TLFontFace): Promise<void> {
+		const existingState = this.getFontState(font)
+		if (existingState) return existingState.loadingPromise
+
+		const instance = this.findOrCreateFontFace(font)
+		// dispose() clears fontStates while loads may still be in flight, so a late
+		// callback must only touch the exact entry it was created for - not a fresh
+		// entry from a later request. Check identity, not just presence.
+		const isStale = () => this.fontStates.__unsafe__getWithoutCapture(font) !== state
+		const state: FontState = {
+			state: 'loading',
+			instance,
+			loadingPromise: instance
+				.load()
+				.then(() => {
+					if (isStale()) {
+						// eslint-disable-next-line no-console
+						console.debug(`Font "${font.family}" load interrupted by editor dispose`)
+						return
+					}
+					this.editor.getContainerDocument().fonts.add(instance)
+					this.fontStates.update(font, (s) => ({ ...s, state: 'ready' }))
+				})
+				.catch((err) => {
+					if (isStale()) {
+						// eslint-disable-next-line no-console
+						console.debug(`Font "${font.family}" load interrupted by editor dispose`, err)
+						return
+					}
+					console.error(err)
+					this.fontStates.update(font, (s) => ({ ...s, state: 'error' }))
+				}),
+		}
+
+		this.fontStates.set(font, state)
+		return state.loadingPromise
+	}
