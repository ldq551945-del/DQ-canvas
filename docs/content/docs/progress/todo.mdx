---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- GitHub 发布部署验收：在干净 Linux 服务器使用新的 `.env.example` 与主 Compose 完成镜像拉取、PostgreSQL 健康检查、`/install` 初始化、管理员创建、静态资源加载、HTTPS 反向代理和数据卷重启恢复；再用 Render Blueprint 验证数据库注入、加密密钥和首次部署。
- 短剧生产线后续：重新构建生产 Docker 镜像，使用真实文本、图片、视频与音频渠道验收两阶段剧本分析、三种视频模式、镜头转码、配音混合、中文字幕烧录、剪映 5.x/6+ 草稿导入、任务取消和成片下载；继续补充整集成片历史、长期成本归档和媒体访问压力测试。
- 上游可用性验收：当前本地启用渠道已配置 `14` 个文本逻辑模型和 `1` 个图片逻辑模型，默认文本为 `gpt-5-5`、默认图片为 `gpt-image-2`；文本单测已确认 `gpt-5-3` 通过 OpenAI 文本兼容协议返回 `200`。图片检测请求已返回 HTTP `200`，但页面结果在开发重载前未留存，仍需重新确认实际图片响应而不能只看路由状态；当前没有视频和音频逻辑模型，必须配置真实渠道后再验证。为 Agnes 和其他供应商补充有效密钥或手工模型后，分别保存文本、图片、Seedance 2.0 视频的目录分页、最小调用、轮询、重试和失败退款证据。
- 创作 Agent 后续：为视频产物增加可信封面/关键帧抽取与多帧复盘，并在现有真实图片检查基础上补充主体、品牌元素和参考素材的视觉相似度判断；继续补齐长期积分归档、镜头版本差异对比、整集历史和成本质量分析。
- 统一生成执行器后续：继续接入并验收上游真实取消，逐项确认生成模型、默认参数、并发和渠道与后台设置一致；在多实例环境压测媒体引用统计与删除竞态。
- 参考素材部署验收：正式环境使用外部可访问的 `NEXT_PUBLIC_SITE_URL`，验证上游读取站内图片、视频和音频、生成一致性、历史恢复与临时文件清理；开发机回环地址不得静默退化为无参考生成。
- 第三方中转后续：基于现有逻辑模型绑定继续增加渠道熔断、权重路由、Webhook 回调和上游鉴权媒体转存，并用实际接入平台逐项维护协议验收样例。
- 安全与并发后续：继续在多实例 PostgreSQL、真实反向代理和支付商回调压力下验收认证写入、退款 claim、事件重放和失败退款；媒体侧使用真实本地大视频与私有 S3 验证用户/签名/资源/IP 分层限流、GET/HEAD、32 MiB Range 分段、拖动播放、并发取消、文件句柄、连接池、出站流量和热点资源 429 误伤。
- 全站性能后续：31 个页面的首屏数据、请求数、分页/缓存、SEO 与已知风险矩阵已经建立，`/assets` 已改为服务端分页，Canvas 已拆分列表摘要与单项目详情；下一步按生产等价 PostgreSQL 的真实慢查询补充逐页响应字节、P50/P95/P99、`EXPLAIN (ANALYZE, BUFFERS)`、连接池等待、索引和正式构建/bundle 证据。
- 异步生成商业验收：使用真实图片、视频、音频和 Agent 渠道验证排队、超时、取消、重试、断线恢复、重复提交、卡死任务接管、失败退款和余额/流水/媒体登记核对；明确最大重试次数、人工补账、上游暂停和降级规则。
- 工程质量后续：引入与 Next.js 16、React 19 兼容的 ESLint 规则和仓库内浏览器 E2E 基线，逐步提高 Route Handler 契约测试覆盖率；避免一次性重构全部路由。
- 商业上线验证：逐个支付商完成沙箱/生产前的下单、回调、重复/乱序/延迟回调、支付超时、全额退款和对账验收；支付宝官方支付与当面付必须分别验证，其中当面付补齐预下单二维码、扫码、二维码过期和异步通知证据。同时使用真实 PostgreSQL 压测并发领券、同券并发下单、取消/超时释放、迟到支付冲突和退款后不返券，核对支付商实收始终等于订单快照。当前自动退款按整笔订单处理，如商业规则需要部分退款，需单独补齐订单、支付流水、积分回收和会计口径。由法律顾问按实际运营主体、服务地区、数据位置和退款规则复核服务条款、隐私政策、退款政策、知识产权与内容责任条款，并确认注册同意方式、协议版本和同意时间留痕；现有页面只能作为产品基础文本。
- 邀请增长生产验收：使用真实 PostgreSQL、至少三个账号和真实/沙箱支付验证邀请访问、注册原子绑定、首单门槛、冷静期维护调度、重复支付回调、并发结算、月度/活动上限、同网络/支付身份风险信号、退款积分反向流水及已锁定/核销优惠券人工复核；上线前确定风控阈值、冻结通知、申诉、误伤恢复和运营值班制度。
- 生产可观测与容量：按部署环境选择可选的错误追踪、结构化日志、主机/容器指标和告警方案，补充数据库连接池使用率、慢查询、API P95/P99、支付成功率和生成成功率监控；完成 100/500/1000 并发分级压测后再决定是否引入 Redis、独立队列或扩大连接池，禁止未经压测机械增加基础设施。
- 商业运营后续：继续做服务器媒体备份与容量告警、整库/媒体/OSS 恢复演练、支付商账单真实样例库、账单自动抓取/周期触发对账、发票税务口径、客服与投诉升级、退款审核和异常支付基础风控。
- 商业安全与服务治理：补齐管理员 MFA/异常登录保护和高风险操作复核，建立模型密钥、支付证书、维护令牌与 OSS 凭据的轮换/吊销/泄露响应；明确拒付与支付争议、数据生命周期、S3 清理、存储带宽成本预警、状态页/故障通知和无障碍验收证据。
- 安全事件与供应商退出：单独形成数据泄露、密钥泄露、越权访问、私有媒体暴露和支付数据异常的分级响应手册，演练隔离、取证、通知、会话撤销、密钥轮换、备份恢复和复盘；同时为模型、支付、SMTP、OSS 和通知供应商保留停服/封号/涨价/跨境变化下的迁移、导出与替代方案。
- 商业上线控制项：验证注册、验证码、密码重置、支付结果和故障通知的实际送达，完成 SPF/DKIM/DMARC、退信投诉处理和通知兜底；隔离开发/预发布/生产的数据库、对象存储、支付商户、模型密钥和回调域名，并演练迁移前备份、冒烟、回滚及回滚后数据兼容；补齐单用户/单任务/单日/全站成本上限和异常熔断。
- 用户权利与内容合规：在已实现注销申请、作品举报/申诉、人工处置和审计记录的基础上，明确正式注销执行、创作数据删除、订单/支付法定保留、媒体引用清理、身份复核与响应时限；建立侵权投诉、人工值班、处置时限、误伤复核和真实演练制度。短剧的用户内容确认 Gate 不能替代平台合规审核。
- 作品社区运营验证：在公开界面已实现点赞/取消、作者关注、公开创作者主页、本人关注/粉丝/点赞 Modal、取关/取消点赞/拉黑、互动通知和 7/30 天榜单的前提下，使用至少两个真实账号与公开作品完成关注/取消、双方拉黑与取消拉黑、列表即时刷新、粉丝关系、主页弹窗、点赞历史、用户/管理员下架、下架后永久删除、源素材保留、封禁联动和榜单稳定性验收；补做 390px/430px 实机布局验证，上线前仍必须补齐反刷监控、内容处置、隐私、审核和值班规则。
- 供应商与安全治理：建立模型、支付、SMTP、对象存储和通知服务的合同/配额/SLA/数据处理清单，明确停服替代、用户内容权属、AI 生成标识、年龄限制和可接受使用政策；完成 AGPL/第三方许可证、依赖与镜像扫描、密钥泄露扫描和漏洞披露流程。
- 增长与体验后续：补充收入、用户增长、留存、付费转化、ARPU 和复购指标口径，建设首次使用引导、首个成功作品路径、套餐升级/续费策略和必要的批量运营能力；可靠进度信息优先展示任务阶段，不伪造上游剩余时间。
